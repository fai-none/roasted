# Current implementation task

M1.3 — static journey and sample receipt ready for founder review before M2. M0 visual/mock foundation and M1 implementation are built and simulator-verified; live adapter remains upcoming.

Done means Home → incoming → accept → labeled mock coaching/retry/code-switch → end → sample receipt → Home works, with decline and an empty receipt staying honest. Mock samples never enter saved learner memory.

Current AI TODOs:
- Copy exact pinned theme/avatar PNG; adapt callback controls' identifiers only. No donor runtime imports.
- Keep a single main-actor call session with guarded actions; preserve a small mock/live boundary, not a provider framework.
- Adapt active-call presentation into the complete mock journey; keep sample labels visible throughout.
- Build, exercise accept/decline/mute/retry/end/Home in simulator, inspect screenshots, and commit the reviewable result.
- Present static journey/tone for the existing human gate. Do not claim approval or advance live usage based on silence.

Dashboard reconciliation is blocked by missing authenticated task tools; Snapshot v6 remains untouched. Evidence is in `docs/implementation-evidence.md`.
